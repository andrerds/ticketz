# TYPE  DATABASE        USER            ADDRESS                 METHOD

# 🔒 Conexão local via Unix socket
local   all             all                                     trust

# ✅ Acesso via IPv4 localhost
host    all             all             127.0.0.1/32            trust

# ✅ Acesso via IPv6 localhost
host    all             all             ::1/128                 trust

# ✅ Redes Docker Coolify (ajuste conforme necessário)
host    all             all             10.0.0.0/8              trust

# ✅ Redes Docker padrão (caso use docker-compose tradicional)
host    all             all             172.16.0.0/12           trust

# ⚠️ DEV ONLY: liberar acesso de qualquer IP (use apenas para debug)
# host    all             all             0.0.0.0/0               trust
